#!/bin/sh
set -e

version=$(sed -n 's/^Current development version: //p' AGENTS.md)
if ! grep -Eq "^[a-z]+\\($version\\): .+" "$1"; then
  echo "Commit message must use type($version): description" >&2
  exit 1
fi
